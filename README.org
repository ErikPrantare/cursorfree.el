#+title: cursorfree.el
A GNU Emacs package for specifying complex and extensible editor
operations.

* Description
:PROPERTIES:
:CUSTOM_ID: description
:END:
cursorfree.el provides a library for composing complex high-level
editor operations.  Examples include yanking to multiple parts of the
buffer simultaneously, following links displayed in non-selected
windows, and deleting a region while keeping whitespace tidy and
without moving point.

This package also provides a spoken command language using [[https://github.com/ErikPrantare/phony.el][phony.el]] to
allow controlling Emacs using your voice.  Being able to do this was
the motivating goal of this package, leveraging the natural richness
possible with spoken language.

This package was inspired by [[https://github.com/cursorless-dev/cursorless/][cursorless]], a package providing similar
faculties for VSCode.

* Contents
:PROPERTIES:
:TOC:      :include all
:CUSTOM_ID: contents
:END:

:CONTENTS:
- [[#description][Description]]
- [[#contents][Contents]]
- [[#setup][Setup]]
  - [[#recommended-dependencies][Recommended dependencies]]
  - [[#installation][Installation]]
  - [[#talon-integration][Talon integration]]
- [[#usage][Usage]]
  - [[#spoken-command-structure][Spoken command structure]]
  - [[#primitives][Primitives]]
  - [[#actions][Actions]]
  - [[#modifiers][Modifiers]]
  - [[#infixes][Infixes]]
- [[#extending-cursorfree][Extending cursorfree]]
- [[#versioning-system][Versioning system]]
:END:

* Setup
:PROPERTIES:
:CUSTOM_ID: setup
:END:
** Recommended dependencies
:PROPERTIES:
:CUSTOM_ID: recommended-dependencies
:END:
- [[https://github.com/ErikPrantare/phony.el][phony.el]] if this package is to be used for controlling Emacs by
  voice.
- [[https://github.com/ErikPrantare/hatty.el][hatty.el]] for a way to query arbitrary positions in the visible
  buffer text.
- [[https://github.com/magnars/multiple-cursors.el][multiple-cursors.el]] for integration with multiple-cursors.  Useful
  when working with multiple targets at once.

** Installation
:PROPERTIES:
:CUSTOM_ID: installation
:END:
To install this package, add the following to your configuration:

#+begin_src elisp
  (use-package cursorfree
    :vc (:url "https://github.com/ErikPrantare/cursorfree.el"
              :branch "master"
              :rev :newest))
#+end_src

** Talon integration
:PROPERTIES:
:CUSTOM_ID: talon-integration
:END:
To use the spoken command language, install [[https://github.com/ErikPrantare/phony.el][phony.el]].  The command
language defined here assumes that the rules ~digit~, ~word~,
~number~, ~any-alphanumeric-key~, and ~symbol-key~ are defined.  This
may be provided by the user or by loading
~phony-talon-community-bindings~ from phony.el.

After that, add the following to your configuration:

#+begin_src elisp
  (use-package cursorfree-phony)
#+end_src

To enable the command language, either utter ~"phony enable cursorfree"~
or add ~(phony-enable-module 'cursorfree)~ to your configuration.

* Usage
:PROPERTIES:
:CUSTOM_ID: usage
:END:
The objects acted upon by the cursorfree operations are called
/targets/.  Targets can be any elisp object, but are most often
objects representing a region in some buffer.

** Spoken command structure
:PROPERTIES:
:CUSTOM_ID: spoken-command-structure
:END:
The typical structure of a command is ~<action> <target>~, with
~<target>~ a sequence of primitives, modifiers, and infixes.

A primitive is something that immediately evaluates to a target.  For
example, ~row 42~ evaluates to a target for line 42.

Modifiers form a pipeline for targets.  ~block~ is a modifier
selecting the paragraph of a target.  Using the aforementioned
primitive, ~row 42 block~ would evaluate to a target for the paragraph
at line 42.

Infixes are like modifiers, but combine two targets.  ~past~ is an
infix selecting a range between two other targets.  ~row 42 past row
67~ would evaluate to a target ranging from lines 42 to 67.

Actions take a target and perform some operation.  For example, ~chuck
row 42 past row 67~ removes lines 42 to 67.

Some actions have a more complex structure.  One commonly used action
with more complex structure is ~bring <target> to <target>~.  It
overwrites the second target with the contents of the first one.  So,
~bring row 13 past row 15 to row 42 block~ would overwrite the
paragraph at line 42 with the text spanning line 13 to 15.

There are a heap of different actions, primitives, modifiers and
infixes, not all of them related to the regions of the buffer.
Following is a list of all available elements for forming a command.

** Primitives
:PROPERTIES:
:CUSTOM_ID: primitives
:END:
A primitive is a target that can be referred to directly.

|-----------------------------+----------------------------------------------|
| Spoken form                 | Description                                  |
|-----------------------------+----------------------------------------------|
| [color] [shape] <character> | Token under a hatty hat (see hatty package). |
| car <character>             | The character itself as a string.            |
| word <word>                 | The word itself as a string.                 |
| numb <number>               | The number itself.                           |
| row <number>                | Line with the given number (modulo 100).     |
| long row <number>           | Line with the given number (exact).          |
| split <number>              | Window with the given [[https://melpa.org/#/winum][winum.el]] index.        |
| this                        | Point, or region if active.                  |
| that                        | The target of the last action.               |
| source                      | The source of the last action.               |
| clip                        | The kill ring.                               |
| primary                     | The primary selection.                       |
| its, itself                 | The result of the last evaluation.           |
|-----------------------------+----------------------------------------------|

** Actions
:PROPERTIES:
:CUSTOM_ID: actions
:END:
An action consumes targets and performs a side effect.  The most
important actions are ~pre~, ~take~, ~change~, ~bring~ and ~move~.

The following table lists the different actions, both the spoken form
and the corresponding library function.

|-------------+--------------------------------------+---------------------------|
| Spoken form | Description                          | Function                  |
|-------------+--------------------------------------+---------------------------|
| take        | Select target                        | cursorfree-select         |
| copy        | Copy to kill ring                    | cursorfree-copy           |
| chuck       | Delete target                        | cursorfree-delete         |
| clone       | Insert a copy after itself           | cursorfree-clone          |
| jump        | Move point to target                 | cursorfree-jump           |
| pre         | Move point to beginning              | cursorfree-jump-beginning |
| post        | Move point to end                    | cursorfree-jump-end       |
| comment     | Comment out                          | cursorfree-make-comment   |
| uncomment   | Uncomment                            | cursorfree-uncomment      |
| indent      | Indent                               | cursorfree-indent         |
| narrow      | Narrow buffer to target              | cursorfree-narrow         |
| title       | Capitalize each word                 | cursorfree-capitalize     |
| upcase      | Convert to upper case                | cursorfree-upcase         |
| downcase    | Convert to lower case                | cursorfree-downcase       |
| crown       | Scroll target to top of window       | cursorfree-crown          |
| center      | Scroll target to center of window    | cursorfree-center         |
| bottom      | Scroll target to bottom of window    | cursorfree-bottom         |
| pick        | Follow or activate thing at target   | cursorfree-pick           |
| fuse        | Remove all whitespace within target  | cursorfree-fuse           |
| tidy        | Tidy up whitespace and formatting    | cursorfree-tidy           |
| join        | Join into one line                   | cursorfree-join           |
| break       | Insert newline before target         | cursorfree-break          |
| flash       | Temporarily highlight                | cursorfree-pulse          |
| help        | Show help at target                  | cursorfree-help           |
| drink       | Insert empty line before, jump there | cursorfree-drink          |
| pour        | Insert empty line after, jump there  | cursorfree-pour           |
| drop        | Insert empty line before             | cursorfree-drop           |
| float       | Insert empty line after              | cursorfree-float          |
| puff        | Insert empty lines around            | cursorfree-puff           |
|-------------+--------------------------------------+---------------------------|

Some actions have more complex spoken forms:

|------------------------------------------------+--------------------------------------+----------------------|
| Spoken form                                    | Description                          | Function             |
|------------------------------------------------+--------------------------------------+----------------------|
| bring <target> to/after/before <target>        | Move copy to/after/before target     | cursorfree-do-bring  |
| move <target> to/after/before <target>         | Move target to/after/before target   | cursorfree-do-move   |
| change <target> [to <target>]                  | Change target [to target]            | cursorfree-do-change |
| swap <target> with <target>                    | Swap target with target              | cursorfree-swap      |
| wrap <target> with <character>                 | Wrap target with character           | cursorfree-wrap      |
| unwrap <target>                                | Remove enclosing delimiter           | cursorfree-unwrap    |
| rewrap <target> with <character>               | Replace enclosing delimiter          | cursorfree-rewrap    |
| hunt <target> [in <target>] [context <number>] | Occur search [in target] [context N] | cursorfree-occur     |
|------------------------------------------------+--------------------------------------+----------------------|

** Modifiers
:PROPERTIES:
:CUSTOM_ID: modifiers
:END:
A modifier transforms a target into a new target.

|-----------------------+------------------------------------------+-------------------------------|
| Spoken form           | Description                              | Function                      |
|-----------------------+------------------------------------------+-------------------------------|
| paint                 | Expand to surrounding non-whitespace     | cursorfree-paint              |
| leftpaint             | Expand left to non-whitespace            | cursorfree-paint-left         |
| rightpaint            | Expand right to non-whitespace           | cursorfree-paint-right        |
| trim                  | Shrink to exclude surrounding whitespace | cursorfree-trim               |
| line                  | Extend to non-whitespace on line         | cursorfree-line               |
| tail                  | Extend to end of line                    | cursorfree-line-right         |
| head                  | Extend to start of line                  | cursorfree-line-left          |
| block                 | Extend to surrounding paragraph          | cursorfree-block              |
| token                 | Extend to containing hatty token         | cursorfree-token              |
| comment               | Extend to containing comment             | cursorfree-comment            |
| string                | Extend to containing string literal      | cursorfree-string-literal     |
| word                  | Extend to containing word                | cursorfree-word               |
| symbol                | Extend to containing symbol              | cursorfree-symbol             |
| sentence              | Extend to containing sentence            | cursorfree-sentence           |
| everything            | Whole buffer contents                    | cursorfree-everything         |
| visible               | Visible portion of buffer                | cursorfree-visible            |
| every instance        | All occurrences of target                | cursorfree-every-instance     |
| next                  | Next occurrence                          | cursorfree-next               |
| preve                 | Previous occurrence                      | cursorfree-previous           |
| beginning             | Empty region at start of target          | cursorfree-beginning          |
| end                   | Empty region at end of target            | cursorfree-end                |
| buffer                | Buffer containing target                 | cursorfree-buffer-or-current  |
| split                 | Window containing target                 | cursorfree-window-or-selected |
| inside [<character>]  | Expand to inside enclosing delimiter     | cursorfree-inside             |
| outside [<character>] | Expand to include enclosing delimiter    | cursorfree-outside            |
|-----------------------+------------------------------------------+-------------------------------|

** Infixes
:PROPERTIES:
:CUSTOM_ID: infixes
:END:
The region spanning two targets can be referred to with ~<target> past
<target>~.

Sometimes, one wants to act on multiple targets.  This can be achieved
with the infix ~and~, which collects targets into a sequence.
Modifiers and actions act on all the targets of the sequence.  For
example, ~chuck row 23 and row 42 block~ creates a sequence with the
targets ~row 23~ and ~row 42~, applies ~block~ to them to get the
paragraph at those lines, and finally deletes those two paragraphs
with ~chuck~.

* Extending cursorfree
:PROPERTIES:
:CUSTOM_ID: extending-cursorfree
:END:
Custom target types can be defined by overriding the generic functions
~cursorfree-get~ and/or ~cursorfree-put~.  This allows many built-in
actions to work on them, particularly "bring".

Custom modifiers and primitives can be added by defining phony rules
that contribute to ~cursorfree-modifier~ and ~cursorfree-primitive~.
Custom actions are added by using ~phony-defun~ and capturing on
~cursorfree-target~.

* Versioning system
:PROPERTIES:
:CUSTOM_ID: versioning-system
:END:
This project uses [[https://semver.org/][semantic versioning]].  While still on 0.y.z, an
increment of y signals breaking changes.
